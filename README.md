# agentic-kit for spec driven development

This repo is a minimal template for guiding AI agents to make safe, spec-driven changes over time with files like `AGENTS.md`, `.agents/INDEX.md`, ...

"Records" based action memory in `.agents/records/<branch>/`.
